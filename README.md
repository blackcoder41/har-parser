# HTTP Archive (HAR) Parser

Saves the response body to a file.

### Dependencies
* `Java` : Java is all you need.

### Platform Specifics
* `Windows` : Tested using Windows 10.
* `Linux` : Not tested.
* `macOS` : Not tested.
